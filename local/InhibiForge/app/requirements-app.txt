# InhibiForge 통합 이미지 — pip 의존성 (GPU + CPU + Streamlit UI)
# 대형 리포(RFdiffusion, ColabDesign, SE3Transformer)는 EBS 볼륨에서 마운트
#
# 설치 순서:
#   1) pip install torch==2.1.0 --index-url https://download.pytorch.org/whl/cu121
#   2) pip install dgl==1.1.3+cu121 -f https://data.dgl.ai/wheels/cu121/repo.html
#   3) pip install -r requirements-app.txt

python-dotenv==1.1.0

# ── GPU: RFdiffusion / ProteinMPNN / AlphaFold ──────────────────────────────
# PyTorch → 별도 설치 (위 순서 참조)
# DGL     → 별도 설치 (위 순서 참조)

# 과학 계산 (GPU + CPU 공통)
numpy==1.26.4
scipy==1.15.3
icecream==2.1.10    # RFdiffusion 내부 로깅

# RFdiffusion 설정 관리
hydra-core==1.3.2
omegaconf==2.3.0
PyYAML==6.0.3
pyrsistent==0.20.0

# SE3 등변 신경망
e3nn==0.5.5

# JAX (AlphaFold / ColabDesign)
jax[cuda12]==0.4.30
jaxlib==0.4.30
dm-haiku==0.0.16
ml-collections==1.1.0

# ColabDesign (ProteinMPNN + AF2)
absl-py
biopython
chex
dm-tree
immutabledict
joblib
matplotlib
optax
pandas

# 기타 (GPU)
opt-einsum==3.4.0
opt-einsum-fx==0.1.4

# ── CPU: LightDock / DNAWorks 도킹 ──────────────────────────────────────────
lightdock==0.9.4
pdbfixer==1.12.0
OpenMM==8.5.0
ProDy==2.6.1
freesasa==2.2.1

# ── Streamlit UI ─────────────────────────────────────────────────────────────
streamlit>=1.30
altair
watchdog>=3.0
py3Dmol==1.8.1      # MPNN/AF2 러너가 옵션 import, 없으면 "py3Dmol not installed" 경고
plotly==5.17.0
pydeck==0.9.1
